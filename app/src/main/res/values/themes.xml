<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BackgroundApp" parent="Theme.Material3.DayNight.NoActionBar"></style>

    <!-- SplashScreen teması -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.BackgroundApp</item>
    </style>

    <style name="Theme.BackgroundApp" parent="Base.Theme.BackgroundApp" />
</resources>